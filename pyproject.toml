[project]
name = "openenv-app"
version = "0.1.0"
description = "OpenEnv FastAPI app"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "gymnasium"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
